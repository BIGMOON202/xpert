import 'package:flutter/cupertino.dart';

class SplashPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const SizedBox();

    // return Column(
    //   mainAxisAlignment: ,
    //   children: [Center(child: Text("Test SSS")),],
    // );//Column
  }
}
